name: Wake Up My Game
on:
  schedule:
    - cron: '0 */2 * * *' # 每 2 小时自动戳一下你的网页
  workflow_dispatch:      # 允许手动运行

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -I -A "Mozilla/5.0" https://recruitment-dashboard-ipmsabadgebtsi8nsngina.streamlit.app/
