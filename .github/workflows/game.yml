name: Wake Up Game
on:
  schedule:
    - cron: '0 */2 * * *' # 每 2 小时自动运行
  workflow_dispatch:      # 允许手动运行

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping All Apps
        run: |
          # 踢醒第一个：Game
          curl -I -A "Mozilla/5.0" https://recruitment-dashboard-ipmsabadgebtsi8nsngina.streamlit.app/
          
          # 踢醒第二个：Supervisor
          curl -I -A "Mozilla/5.0" https://recruitment-dashboard-kyhnhmdicnr6qjeamripzs.streamlit.app/
